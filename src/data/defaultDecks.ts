import type { Card, Deck } from '../types/flashcard';
import { CATEGORY_DECKS, CATEGORY_CARDS } from './categoryDecks';

export const INITIAL_DECKS: Deck[] = CATEGORY_DECKS;
export const INITIAL_CARDS: Card[] = CATEGORY_CARDS;
